<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">


	<mapper namespace="consumerMapper">
	
		<select id="isConsumerId" resultType="consumer" parameterType="string">
			SELECT
	 	       	   Consumer_NO consumerNo
	 	       	 , Consumer_ID consumerId
	 	       	 , Consumer_PW consumerPw
		  	  FROM
		           Consumer
		 	 WHERE
		           Consumer_ID = #{id}
		</select>
		
		
		<insert id="createConsumer" parameterType="consumer">
			 INSERT
		 	   INTO
		      	    Consumer 
		      	    (
					Consumer_ID
		      	  , Consumer_PW
		      	    )
		     VALUES
				    (
				    #{consumerId}
				  , #{consumerPw} 
				    )
		</insert>

		<select id="login" resultType="consumer" parameterType="consumer">
				SELECT
			   Consumer_NO consumerNo
			 , Consumer_ID consumerId
			 , Consumer_PW consumerPw
		  FROM
		       Consumer
		 WHERE 
		       Consumer_ID = #{consumerId}
		   AND
		       Consumer_PW = #{consumerPw}
		</select>


	</mapper>