<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace ="restaurantMapper">
  
  	<select id="selectRestaurants" resultType="restaurant">
  		SELECT
  		       REST_NO
  		     , REST_NAME
  		     , CATEGORY
  		     , MIN_PRICE
  		     , DELIVERY_FEE
  		  FROM
  		       RESTAURANT
  		  JOIN
  		       REVIEW USING()
  		 ORDER
  		    BY
  		       REST_NO
  	</select>
  
  	<select id="selectAvgStar" resultType="restaurant">
		SELECT
		 	   REST_NO
		     , AVG(REVIEW_STAR)
		  FROM
		       REVIEW
		  JOIN 
		       MENU USING(REST_NO)
		  JOIN
		       ORDERS USING(MENU_NO)
		 GROUP
		    BY
		       REST_NO
  	</select>

  </mapper>